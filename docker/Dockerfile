# Вибираємо базовий образ — мінімалістичний Alpine Linux
FROM alpine:latest

# Команда, яка виконується при запуску контейнера
CMD ["echo", "Привіт з Docker 🎉"]
